tinymce.PluginManager.add('example', function(editor, url) {
    // 注册一个工具栏按钮名称
    editor.ui.registry.addButton('example', {
      text: '插入一段文字',
      onAction: function () {
          editor.focus(); 
          editor.selection.setContent(`<br/><strong>插入一段文字</strong>`); 
      }
    });
});
  